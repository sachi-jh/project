Download the repo here if our upload corrupted for whatever reason: https://github.com/sachi-jh/project

To run backend:
cd backend
npm install
npm run start

To run frontend:
cd frontend
npm install
npm run dev

Node packages will eventually be combined when frontend becomes static